<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lucky.payment.mapper.PaymentMapper">

    <resultMap id="paymentResultMap" type="com.lucky.payment.domain.Payment">
        <id property="id" column="id"/>
        <result property="paymentId" column="payment_id"/>
        <result property="memberId" column="member_id"/>
        <result property="productCode" column="product_code"/>
        <result property="orderName" column="order_name"/>
        <result property="amount" column="amount"/>
        <result property="currency" column="currency"/>
        <result property="payMethod" column="pay_method"/>
        <result property="status" column="status"/>
        <result property="pgTxId" column="pg_tx_id"/>
        <result property="rawJson" column="raw_json"/>
        <result property="inputJson" column="input_json"/>
        <result property="paidAt" column="paid_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.lucky.payment.domain.Payment"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO payments
            (payment_id, member_id, product_code, order_name, amount, currency, status)
        VALUES
            (#{paymentId}, #{memberId}, #{productCode}, #{orderName}, #{amount}, #{currency}, #{status})
    </insert>

    <select id="findByPaymentId" resultMap="paymentResultMap">
        SELECT * FROM payments WHERE payment_id = #{paymentId}
    </select>

    <select id="existsPaidByMemberAndProduct" resultType="boolean">
        SELECT EXISTS(
            SELECT 1 FROM payments
            WHERE member_id = #{memberId}
              AND product_code = #{productCode}
              AND status = 'PAID'
        )
    </select>

    <update id="updateResult" parameterType="com.lucky.payment.domain.Payment">
        UPDATE payments
        SET status     = #{status},
            pay_method = #{payMethod},
            pg_tx_id   = #{pgTxId},
            raw_json   = #{rawJson},
            paid_at    = #{paidAt},
            updated_at = NOW()
        WHERE payment_id = #{paymentId}
    </update>

    <!--
      결제는 됐는데(PAID) 리포트 행이 없는 결제건.
      결제 직후 브라우저를 닫는 등으로 생성 요청이 유실된 경우를 보관함에서 복구하기 위한 조회.
    -->
    <select id="findUnfulfilledByMember" resultMap="paymentResultMap">
        SELECT p.* FROM payments p
        LEFT JOIN fortune_results r ON r.payment_id = p.payment_id
        WHERE p.member_id = #{memberId}
          AND p.status = 'PAID'
          AND r.id IS NULL
        ORDER BY p.paid_at DESC NULLS LAST, p.id DESC
    </select>

</mapper>
